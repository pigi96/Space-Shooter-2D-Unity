using UnityEditor;
using UnityEngine;

public enum Direction
{
    None = 0,
    Left = 1,
    Right = 2,
}

public enum BulletType
{
    Enemy = 0,
    Player = 1,
}

public enum PowerUpType
{
    Repair = 0,
    Armor = 1,
}